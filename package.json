{
  "name": "rebecca-bridge",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run --reporter=basic",
    "lint:sol": "solhint 'contracts/**/*.sol'"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "@types/node": "^20.11.30",
    "solhint": "^4.5.4"
  }
}
